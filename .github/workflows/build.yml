name: Build Extension

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create zip
        run: zip -r fast-bookmark-ext.zip manifest.json background.js content.js icons/ COPYING README.md

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: fast-bookmark-ext
          path: fast-bookmark-ext.zip

      - name: Upload to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: fast-bookmark-ext.zip
